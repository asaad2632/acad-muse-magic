name = "asaad2632-acad-muse-magic"
compatibility_date = "2026-07-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "dist"

# Real secrets/env vars (DATABASE_URL, R2_*, GROQ_API_KEY, etc.) are set via
# the Cloudflare Pages dashboard (Settings -> Environment variables), never
# committed here. This file only carries build/runtime settings Cloudflare
# needs before running the build command.
